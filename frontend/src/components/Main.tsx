import SectionHome from "./SectionHome";
import CardRow from "./CardRow";

export default function Main() {
  return (
    <main>
      <SectionHome />
      <CardRow />
      <CardRow />
    </main>
  );
}
